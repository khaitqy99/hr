# Supabase CLI Configuration
# File này cấu hình cho Supabase CLI

[functions.send-otp-email]
# Cho phép gọi function mà không cần verify JWT
# Lưu ý: Vẫn cần có authorization header với anon key
verify_jwt = false

[functions.send-shift-change-notification]
verify_jwt = false

[functions.send-web-push]
# Database Webhook gọi bằng service role / không có user JWT
verify_jwt = false
