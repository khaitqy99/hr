# Copy thành .env và điền giá trị thật (file .env đã được gitignore)

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Public key từ: npx web-push generate-vapid-keys
# Private key chỉ đặt trên Supabase secrets (VAPID_PRIVATE_KEY), không đưa vào frontend
VITE_VAPID_PUBLIC_KEY=
